{
  "skill_id": "movies_showtimes",
  "name": "Movie Showtimes",
  "is_searchable": true,
  "search_config": {
    "corpus_target": "response_body",
    "budget_tokens": 1200
  },
  "intents": ["get_showtimes"],
  "categories": ["current_media"],
  "description": "Find current movie showtimes using live web search results. Best for 'what time is Avatar playing tonight' style asks.",
  "examples": [
    "what time is avatar playing tonight",
    "show me movie times for dune in brooklyn",
    "are there any screenings for wicked today"
  ],
  "parameters": {
    "query": {"type": "string", "required": true}
  },
  "config_schema": {
    "global": [],
    "user": [
      {
        "key": "default_location",
        "type": "string",
        "label": "Default location",
        "description": "Used to localize showtime lookups when the query does not already include a city or area.",
        "default": "",
        "required": true
      }
    ]
  },
  "mechanisms": [
    {
      "method": "ddg_showtimes",
      "priority": 1,
      "timeout_ms": 5000,
      "requires_internet": true
    },
    {
      "method": "local_cache",
      "priority": 2,
      "timeout_ms": 500,
      "requires_internet": false
    }
  ]
}
